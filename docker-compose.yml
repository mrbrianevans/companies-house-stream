version: '3'
services:
  redis-cache:
    image: redis
  stream-server:
    build: .
    ports:
      - "80:3000"
      - "443:3000"
    env_file:
      # postgres connection details
      - .env
  mongo-cache:
    image: mongo
    command: --quiet



